{
  "name": "useless-functions",
  "version": "1.0.4",
  "description": "A series of useless functions for understanding ES6 import export",
  "repository": {
    "type": "git",
    "url": "https://github.com/halversondm/useless-functions"
  },
  "main": "lib/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prebuild": "npm run clean && npm run eslint",
    "build": "babel src -d lib",
    "eslint": "eslint src/**/**.js --fix",
    "clean": "rimraf ./lib",
    "prepublish": "npm run build"
  },
  "author": "Dan Halverson",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.10.1",
    "babel-preset-es2015": "^6.9.0",
    "babel-preset-react": "^6.5.0",
    "eslint": "^2.12.0",
    "eslint-config-google": "^0.5.0",
    "eslint-plugin-react": "^5.1.1",
    "rimraf": "^2.5.2"
  },
  "dependencies": {
    "react": "^15.1.0",
    "react-dom": "^15.1.0"
  }
}
